import 'package:flutter/widgets.dart';

class ErrorTerminal extends StatelessWidget {
  const ErrorTerminal({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
